{
  "name": "@loage/example-home-automation",
  "private": true,
  "type": "module",
  "scripts": {
    "flow": "node build-flow.ts > flow.json",
    "devices": "node fake-devices.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": { "@loage/core": "workspace:*", "@loage/providers": "workspace:*" }
}
