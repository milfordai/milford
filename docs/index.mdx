---
title: "Introduction"
description: "Milford is a headless workflow engine for typed decisions, model calls and HTTP calls, wired in a graph."
---

Milford runs flows: graphs of small steps such as typed decisions, model calls, templates and HTTP calls. You configure it as code, run it as a TypeScript library or a single Docker container, and call it over HTTP.

Milford is not an agent framework. There are no agent loops and no tool-use planning. A flow is a fixed graph that you can read, version in git and test.

## What you get

- A core library, `@milford/core`, with no I/O and no vendor knowledge.
- Provider adapters in `@milford/providers`: OpenAI (and compatible servers), Anthropic, Jev and generic HTTP.
- A thin HTTP server, `@milford/server`, with bearer auth and server-sent events.
- An MCP server, `@milford/mcp`, that exposes flows as tools to external LLMs.
- A `Dockerfile` for `linux/amd64` and `linux/arm64`. No database.

## Where to go next

<Columns cols={2}>
  <Card title="Quickstart" icon="rocket" href="/quickstart">Run your first flow.</Card>
  <Card title="Flows" icon="workflow" href="/concepts/flows">How the executor walks a graph.</Card>
  <Card title="Decisions" icon="git-branch" href="/concepts/decisions">Typed answers with probabilities.</Card>
  <Card title="Home automation" icon="house" href="/guides/home-automation">A free-text command turned into device actions.</Card>
</Columns>
