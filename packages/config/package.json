{
  "name": "@loage/config",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "loage.config.schema.json"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node dist/gen-schema.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": { "@loage/core": "workspace:*", "yaml": "^2.9.1", "zod": "^4.6.5" }
}
