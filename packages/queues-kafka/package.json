{
  "name": "@milfordai/queues-kafka",
  "version": "0.0.3",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@milfordai/core": "workspace:*",
    "kafkajs": "^2.2.4",
    "zod": "^4.6.5"
  },
  "description": "Milford queue adapter: Apache Kafka consumers that run a flow per message, with retries, dead letters and idempotent redelivery.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/milfordai/milford.git",
    "directory": "packages/queues-kafka"
  },
  "homepage": "https://milford.mintlify.site",
  "bugs": {
    "url": "https://github.com/milfordai/milford/issues"
  },
  "keywords": [
    "milford",
    "workflow",
    "kafka",
    "queue",
    "consumer"
  ],
  "engines": {
    "node": ">=22"
  },
  "publishConfig": {
    "access": "public"
  }
}
