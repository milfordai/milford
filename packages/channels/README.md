# @loage/channels

Chat and webhook entry points for [Loage](https://github.com/loage-ai/loage): Slack (Socket Mode), Telegram (long polling) and HMAC-signed webhooks. Access is deny by default. Normally you configure them through `@loage/server`.

See the [channels guide](https://loage.mintlify.site/guides/channels).
